name: Build and Deploy to Sandbox

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout this repo
      uses: actions/checkout@v4
    - name: Cache dependencies
      uses: actions/cache@v3
      with:
        path: ~/.m2/repository
        key: ${{ runner.os }}-maven-${{ hashFiles('**/pom.xml') }}
        restore-keys: |
          ${{ runner.os }}-maven-
    - name: Set up JDK 17
      uses: actions/setup-java@v3
      with:
        distribution: 'temurin'
        java-version: 17
    - name: Build with Maven
      run: mvn -B package --file pom.xml
    - name: Stamp artifact file name with commit hash
      run: |
        set -euo pipefail
        artifact=$(ls target/*.jar | head -n1)
        commitHash=${GITHUB_SHA::7}
        newArtifact="${artifact%.jar}-$commitHash.jar"
        mv "$artifact" "$newArtifact"
    - name: Upload artifact
      uses: actions/upload-artifact@v4
      with:
        name: artifacts
        path: target/*.jar
  upload:
    needs: build
    runs-on: ubuntu-latest
    env:
      CONNECTED_CLIENT_ID: ${{ secrets.connected_client_id }}
      CONNECTED_CLIENT_SECRET: ${{ secrets.connected_client_secret }}

    steps:    
    
    - uses: actions/checkout@v4
    
    - uses: actions/cache@v3
      with:
        path: ~/.m2/repository
        key: ${{ runner.os }}-maven-${{ hashFiles('**/pom.xml') }}
        restore-keys: |
          ${{ runner.os }}-maven-
    
    - uses: actions/download-artifact@main
      with:
        name: artifacts
    
    - name: Upload to Exchange
      run: |
        artifactName=$(ls *.jar | head -1)
        mvn deploy \
         -s .maven/settings.xml \
         -Dmule.artifact=$artifactName \
  
  deploy:
    needs: build
    runs-on: ubuntu-latest
    steps:
    - name: Checkout this repo
      uses: actions/checkout@v4
    - name: Cache dependencies
      uses: actions/cache@v3
      with:
        path: ~/.m2/repository
        key: ${{ runner.os }}-maven-${{ hashFiles('**/pom.xml') }}
        restore-keys: |
          ${{ runner.os }}-maven-
    - name: Set up JDK 17
      uses: actions/setup-java@v3
      with:
        distribution: 'temurin'
        java-version: 17
    - uses: actions/download-artifact@main
      with:
        name: artifacts
        path: .
    - name: Deploy to Sandbox
      env:
        USERNAME: ${{ secrets.anypoint_platform_username }}
        PASSWORD: ${{ secrets.anypoint_platform_password }}
        CONNECTED_CLIENT_ID: ${{ secrets.connected_client_id }}
        CONNECTED_CLIENT_SECRET: ${{ secrets.connected_client_secret }}
        ENVIRONMENT: ${{ secrets.anypoint_environment }}
        APP_NAME: ${{ secrets.APP_NAME }}
      run: |
        artifactName=$(ls *.jar | head -n1)
        mvn clean deploy -DmuleDeploy \
          -DskipTests \
          -DskipDeploymentVerification="true" \
          -Dmule.artifact="$artifactName" \
          -Danypoint.username="$USERNAME" \
          -Danypoint.password="$PASSWORD" \
          -Dap.conn.app.client.id="$CONNECTED_CLIENT_ID" \
          -Dap.conn.app.client.secret="$CONNECTED_CLIENT_SECRET" \
          -DappName="$APP_NAME" \
          -Dapp.env="$ENVIRONMENT"